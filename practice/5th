import re

text = input("Enter a text: ")

pattern = r'₹\d+(?:,\d{3})*(?:\.\d{2})?|\$\d+(?:,\d{3})*(?:\.\d{2})?'

currency = re.findall(pattern, text)

if currency:
    print("Currency values found:")
    for value in currency:
        print(value)
else:
    print("No currency values found.")
